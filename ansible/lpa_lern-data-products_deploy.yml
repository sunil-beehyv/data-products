---
- hosts: "{{remote}}"
  vars_files:
    - "{{inventory_dir}}/secrets.yml"
  become: yes
  become_user: "{{ analytics_user }}"
  environment:
    AWS_ACCESS_KEY_ID: "{{sunbird_private_storage_account_name}}"
    AWS_SECRET_ACCESS_KEY: "{{sunbird_private_storage_account_key}}"
    OCI_CLI_AUTH: "instance_principal"
  roles:
   - lern-data-products-deploy
